# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Nexus memory (local only)
MEMORY.md

# Nexus runtime (don't commit)
.nexus/
logs/
run/
*.db
*.log

# Playwright test artifacts
.playwright-mcp/
_temp/
screenshots/
test_message.txt

# Frontend
# 注意：package-lock.json 应入库以保证依赖复现
node_modules/
frontend/node_modules/

# Local config
.claude/settings.local.json
.claude/
.pytest_cache/

# Vite 缓存（不应入 git；历史误建在 nexus/.vite/）
.vite/
**/.vite/

# 测试 artifacts（兜底）
frontend/tests/

# 本地 codegraph 索引 + 临时实验（不入库）
.codegraph/
experiments/

# PyInstaller 构建产物 + spec（不入库；发布走 GitHub Releases）
build/
dist/nexus-backend
min-spec.spec
nexus-backend.spec
*.spec

# macOS DMG 发布产物（本地保留，发布走 GitHub Releases）
release/*.dmg
release/*.dmg.blockmap
release/builder-debug.yml

# Electron / DMG build 输出
release/
desktop/dist/

# Full-stack verify scripts 截图产物(verify-full.js 用)
.verify-shots/

# Playwright 自动产物(root + frontend)
test-results/
frontend/test-results/
playwright-report/

# 调试 / 手动测试 截图产物(E2E debug spec + manual UI flow 用)
.e2e-shots/
frontend/debug-shots/
frontend/manual-shots/

# 实验性 staging 目录(本地 .laobai-lab / 反复跑出的产物,不入库)
.laobai-lab-staging/

# Tauri 编译产物(desktop/src-tauri/frontend-dist 由 vite build 出来,产物)
desktop/src-tauri/frontend-dist/
