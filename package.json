{
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:dmg": "bash scripts/build_dmg.sh",
    "build:all": "npm run build:frontend && npm run build:dmg",
    "verify:full": "node verify-full.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "puppeteer": "^25.1.0"
  }
}